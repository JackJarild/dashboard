import { LoginLayout } from '../components/LoginLayout';
import { LoginForm } from '../components/LoginForm';

export const Login = () => {
  return (
    <LoginLayout>
      <LoginForm />
    </LoginLayout>
  );
};